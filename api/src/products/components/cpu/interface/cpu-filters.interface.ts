export interface CPUFilters {
    generation?: string
    series?: string
    socket?: string
    ramType?: string
    ramCapacity?: number
    ramChannels?: number
    minPrice?: number
    maxPrice?: number
    brand?: string
}
